<!--
FEDCON, one process for every repo (Brad, 2026-08-06).

Before you open this: add ONE disposition label. It is the only thing that tells a reader
whether this wants a merge, and GitHub's PR list does not show body text.

  finalize-ready       done and verified, a human should merge it
  redlines-in-flight   unfinished -> open it as a DRAFT, not a normal PR
  close-dont-merge     never merges; close it once folded in or superseded
  needs-brad           a decision only Brad can make, and it could not be resolved in a reply

A PR is a request to merge. If you are not asking for a merge, open a draft.
Never open a normal PR that waits on a person and then walk away.

`/merge-queue` shows the whole org bucketed by what a human actually has to do.
Full process: the Claude Code cheat sheet, section 7.
-->

**Waiting on:**
<!-- Delete this line ONLY if the label is finalize-ready. Otherwise name a person or an
     event, never a status word. "In progress" is not a blocker.
       Waiting on: Brad to rule on the two candidates
       Waiting on: a rebase onto main (conflict in tools/new_prospect.py)
       Waiting on: CI to go green                                                        -->

## What this changes

<!-- Lead with the outcome. What is true after this merges that was not true before. -->

## Why

<!-- The problem it solves. If a decision was made, say what it was and what it rules out. -->

## Evidence

<!-- What you ran, and what it returned. Tests, a probe, a rendered page, a live read.
     "Should work" is not evidence. If nothing was verified, say so plainly. -->
